exports.createBooking = (req, res) => {
  res.json({ message: 'Create booking (to implement)' });
};

exports.getUserBookings = (req, res) => {
  res.json({ message: 'Get user bookings (to implement)' });
};

exports.getAllBookings = (req, res) => {
  res.json({ message: 'Get all bookings (to implement)' });
}; 